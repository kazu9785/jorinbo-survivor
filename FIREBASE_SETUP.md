# Firebase設定手順

このゲームは、Firebase Authenticationで「名前＋パスワード」のログインを行い、Cloud Firestoreで全員のランキングを共有します。

## 1. Firebaseプロジェクトを作る

1. [Firebaseコンソール](https://console.firebase.google.com/)を開く
2. 「プロジェクトを作成」を押す
3. 分かりやすい名前を付けて作成する
4. プロジェクト画面の「ウェブ」アイコン `</>` を押す
5. アプリ名を入力して「アプリを登録」を押す
6. 表示された `firebaseConfig` の各値を控える

## 2. firebase-config.jsへ設定を貼る

`firebase-config.js` をVS Codeで開き、`PASTE_YOUR_...` と書かれた6つの値をFirebaseコンソールに表示された値へ置き換えます。

値の前後の `"` と、行末のカンマは残してください。Firebaseの設定値はWebアプリ上に公開される識別情報です。サービスアカウントの秘密鍵や自分のパスワードは絶対に書かないでください。

## 3. 名前＋パスワード認証を有効にする

1. Firebaseコンソール左側の「構築」→「Authentication」を開く
2. 「始める」を押す
3. 「Sign-in method」タブを開く
4. 「メール/パスワード」を選ぶ
5. 1つ目の「メール/パスワード」を有効にして保存する

ゲーム画面ではメールアドレスを入力させません。入力された名前を内部専用のメール形式に変換してFirebase Authenticationへ渡します。

## 4. Cloud Firestoreを作る

1. 左側の「構築」→「Firestore Database」を開く
2. 「データベースの作成」を押す
3. 「本番環境モード」を選ぶ
4. 利用者に近いロケーションを選んで作成する

## 5. セキュリティルールを設定する

1. Firestore Databaseの「ルール」タブを開く
2. エディター内のコードをすべて削除する
3. このフォルダーの `firestore.rules` の全文を貼る
4. 「公開」を押す

このルールでは、ランキングは全員が読めますが、書き込みはログインした本人の記録だけに制限されます。

## 6. GitHub Pagesのドメインを登録する

1. Firebase Authenticationの「Settings（設定）」を開く
2. 「Authorized domains（承認済みドメイン）」を開く
3. GitHub Pagesのドメイン `あなたのGitHub名.github.io` が無ければ追加する

`https://あなたのGitHub名.github.io/リポジトリ名/` の場合も、追加するのはパスを除いた `あなたのGitHub名.github.io` です。

## 7. GitHubへアップロードする

このフォルダー内のファイルをすべてリポジトリへアップロードします。最低でも次が必要です。

- `index.html`
- `style.css`
- `script.js`
- `firebase-config.js`
- すべての画像ファイル

`firestore.rules` と `FIREBASE_SETUP.md` はゲーム実行には使いませんが、設定の控えとして一緒に置いて構いません。

## 8. 動作確認

1. GitHub PagesのゲームURLを開く
2. 1人目を新規登録する
3. 戦闘開始を押して、ゲームオーバーまたはWAVE20クリアまで遊ぶ
4. ランキングに最高WAVE・直近WAVE・挑戦回数が表示されることを確認する
5. シークレットウィンドウか別のPCで2人目を登録し、同じランキングが見えるか確認する
6. データリセット後、そのプレイヤーの3項目が0になることを確認する
7. テスト用アカウントで「アカウント削除」を押し、パスワード確認後にログイン画面へ戻ることを確認する

VS Codeで確認するときは、`index.html` をダブルクリックして直接開くのではなく、Live Serverなどで `http://localhost:...` として開いてください。

## 記録される内容

| 項目 | 更新タイミング |
| --- | --- |
| 最高WAVE | ゲームオーバーまたはクリア時。過去最高より高い場合だけ更新 |
| 直近WAVE | ゲームオーバーまたはクリア時。最後に到達したWAVEを保存 |
| 挑戦回数 | 「戦闘開始」または「もう一度」を押した時に1増加 |

## 注意

- パスワードを忘れたときの再発行機能は、この「名前だけでログインする簡易方式」にはありません。
- データリセットはアカウントを消しません。強化、コイン、最高WAVE、直近WAVE、挑戦回数を初期状態へ戻します。
- 「アカウント削除」は、パスワードで本人確認した後にAuthenticationのログイン情報、Firestoreのランキング記録、端末内のゲームデータを削除します。
- アカウント削除機能を使うには、最新版の`firestore.rules`をFirebaseコンソールで再度貼り付けて「公開」してください。古いルールのままだとランキング記録を削除できません。
- ランキング値の改ざんを他人から防ぐルールは入っていますが、ブラウザゲームなので本人が開発者ツールを使った不正を完全に防ぐことはできません。本格運用では記録判定をサーバー側へ移す必要があります。
